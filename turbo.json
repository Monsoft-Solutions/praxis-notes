{
    "$schema": "https://turbo.build/schema.json",

    "tasks": {
        "build": {
            "dependsOn": ["^build"]
        },

        "generate": {
            "outputs": ["drizzle"]
        },

        "api:build": {},

        "dev": {
            "cache": false,
            "persistent": true
        },

        "lint": {
            "outputs": [],
            "cache": true
        }
    }
}
