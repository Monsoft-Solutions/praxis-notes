{
    "$schema": "https://turbo.build/schema.json",

    "extends": ["//"],

    "tasks": {
        "build": {
            "outputs": ["bin/**"]
        },

        "api:build": {
            "outputs": ["../packages/template-api/bin/**"]
        },

        "dev": {
            "cache": false,
            "persistent": true
        }
    }
}
