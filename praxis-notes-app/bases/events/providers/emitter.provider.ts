import EventEmitter from 'events';

// -------------------------------------------------------------------------

// centralized emitter used to emit and listen to events
export const emitter = new EventEmitter();
